# The TIA - Team!

steps:
   
- task: PowerShell@2
  condition: eq(variables['Agent.OS'], 'Windows_NT')
  inputs:
    pws: true
    targetType: 'inline'
    script: |          
      "Este es un pipeline para probar la disponibilidad de los microsoft agents. Se ejecutó correctamente."
  displayName: 'Running - Tests '
  timeoutInMinutes: 5 
